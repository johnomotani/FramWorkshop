On Windows: Download the worksheet pdf at https://github.com/johnomotani/FramWorkshop/blob/wsl/NumericalPhysicsWorksheet.pdf onto the Desktop.

sudo apt install git

On Linux:
git clone -b linux https://github.com/johnomotani/FramWorkshop.git
cp FramWorkshop/NumericalPhysicsWorksheet.pdf Desktop/

On WSL:
git clone -b wsl https://github.com/johnomotani/FramWorkshop.git

On both:
echo "Host archer
    HostName login.archer2.ac.uk
    User snewton
" >> ~/.ssh/config
sudo apt install kate openmpi-dev
curl -L -O "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-$(uname)-$(uname -m).sh"
bash Miniforge3-$(uname)-$(uname -m).sh
<accept the license and say 'yes' to all the prompts>
<open a new shell so conda environment is active, and close the old one>
conda config --set changeps1 false
<open a new shell to refresh the prompt, and close the old one>
mamba install animatplot
git clone https://github.com/mabarnes/moment_kinetics.git
# Something about our moment_kinetics setup on julia-1.12 makes printing to the terminal super slow. Install julia-1.11 instead as a work-around.
curl -fsSL https://install.julialang.org | sh -s -- --default-channel 1.11
<Accept the default response at the prompt>
<open a new shell so julia is available, and close the old one>
cd moment_kinetics
machines/machine_setup.sh
<Choose the default option to all the prompts>
bin/julia --project -O3 precompile.jl
rm -r performance-tests plot_comparison.jl plots_post_processing post_process_parameter_scan.jl precompile_dependencies.jl precompile-kinetic-electrons.jl precompile-makie-post-processing.jl precompile-no-run.jl precompile-plots-post-processing.jl precompile-with-check-bounds.jl profiling publication_inputs run_evolve_convergence_test.jl run_makie_post_processing.jl run_MMS_test.jl run_parameter_scan.jl run_post_processing.jl test_scripts



ARCHER2 reservation info
------------------------

Reservation Name: e281-bout_1729862

Consortium: e281 - Plasma Physics Consortium
Requester: Dr John Omotani john.omotani@ukaea.uk
Machine: archer2
Group: e281-bout
Start: Fri Mar 20 09:00:00 GMT 2026
End: Fri Mar 20 16:00:00 GMT 2026
Nodes: 6
